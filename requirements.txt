# Core dependencies for the ClipThis Django app.
# Install with: pip install -r requirements.txt

# Web framework
Django==5.2

# PostgreSQL driver (for Vultr DB)
# Using the prebuilt binary wheels avoids compilation issues.
psycopg[binary]>=3.1,<4

# Authentication (Google OAuth, signup/login flows)
django-allauth>=0.51,<1

# Production WSGI server
gunicorn>=21,<22

# HTTP client
requests>=2.31,<3

# JWT support (import jwt)
PyJWT>=2.8,<3

# Crypto primitives (required by some auth/libs)
cryptography>=42,<43
