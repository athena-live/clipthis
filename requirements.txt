"""
Core dependencies for the ClipThis Django app.
Install with: pip install -r requirements.txt
"""

# Web framework
Django==5.2

# PostgreSQL driver (Vultr DB)
# For easy installation across platforms, use the prebuilt binary wheels.
psycopg[binary]>=3.1,<4

